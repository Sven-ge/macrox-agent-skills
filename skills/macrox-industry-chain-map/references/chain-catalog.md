# 产业链目录（公开、可复述）

先把用户主题归一到下表 **一条链**。目录是默认骨架，不是交易所全集；用户指定的品种用 `ref_list_symbols` 校验后可增补，未校验的代码不得写入 HTML。

## 有色

| 链 id | 名称 | 上游/原料 | 中游 | 下游/相关 |
| --- | --- | --- | --- | --- |
| `cu-chain` | 铜 | `CU` | — | 可用仓单/基差代理需求松紧 |
| `al-chain` | 铝 | `AL`（氧化铝若用户点名再 `ref_list_symbols` 查 `AO`） | `AL` | — |
| `zn-pb-chain` | 锌铅 | `ZN`, `PB` | — | — |
| `ni-ss-chain` | 镍—不锈钢 | `NI` | `SS` | — |
| `sn-chain` | 锡 | `SN` | — | — |
| `precious` | 贵金属（仅用户明确要时） | `AU`, `AG` | — | 不与工业链混写利润 |

用户只说「有色」：默认 `cu-chain` + `al-chain` + `zn-pb-chain` + `ni-ss-chain` 四条做 **目录总览**（每条 1–2 个核心品种），不要一次拉 20 个品种。

## 黑色

| 链 id | 名称 | 原料 | 成材/相关 |
| --- | --- | --- | --- |
| `steel-chain` | 钢（默认黑色） | `I`, `JM`, `J` | `RB`, `HC` |
| `glass-soda` | 玻璃纯碱 | — | `FG`, `SA` |
| `ferroalloy` | 铁合金 | — | `SF`, `SM` |

用户只说「黑色」：默认 `steel-chain`。

## 能化

| 链 id | 名称 | 节点（按需取，先取 4–8 个） |
| --- | --- | --- |
| `oil-chain` | 油品 | `SC`, `FU`, `BU`, `LU` |
| `polyester-chain` | 聚酯 | `TA`, `EG`, `PF` |
| `olefin-chain` | 烯烃/聚烯烃 | `L`, `PP`, `V`, `EB` |
| `coalchem-chain` | 煤化工 | `MA`, `UR` |

用户只说「能化」：问一次选聚酯 / 烯烃 / 油品 / 煤化工，禁止一条 HTML 塞全板块。

## 选取纪律

- 单份交付 **一条主链**（总览最多四条有色短目录）。  
- 每环节至少 1 个代表 `symbol`；取数失败保留环节名，数值列「未取到」。  
- 不要为「看起来完整」编造目录外品种。
